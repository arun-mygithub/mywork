// // alert('NO PROBLEM HERE');

 // $('#notice').each{ |notice| notice.hide }

//  $(document).ready(function(){
//    setTimeout(function(){
//       $("p.notice").hide("slow");
//    },1000);
// });



 $('#cart').html("<%= escape_javascript(render(@cart)) %>");

 $('#cart').css({ 'background-color': "#88ff88" }).animate({ 'background-color': "#114411" }, 4000);
  $("#<%= params[:product_id] %> span").html("<%= @product.initial_quantity %>");
// page.select("#notice" ).each { |notice| notice.hide }
//  page[:cart].visual_effect :blind_down if @cart.total_items == 1
// page[:current_item].visual_effect :highlight,
// :startcolor => "#88ff88" ,
// :endcolor => "#114411"

